name: resqstats
version: "1.0.0"
profile: resqstats

model-paths: ["models"]
test-paths: ["tests"]
target-path: target

models:
  resqstats:
    staging:
      +materialized: view
    marts:
      +materialized: table
